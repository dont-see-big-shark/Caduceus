name: openclaw_protocol
description: Dart client for the OpenClaw gateway WebSocket protocol (v4).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  cryptography: ^2.7.0
  meta: ^1.15.0
  web_socket_channel: ^3.0.1
  caduceus_native:
    path: ../caduceus_native

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
