name: agent_core
description: Backend-neutral model of an agent conversation, and the interface an agent backend implements.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
